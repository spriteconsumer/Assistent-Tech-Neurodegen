{
  "name": "saathi-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for the dementia-support AI companion demo (chat + routine anomaly detection).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
